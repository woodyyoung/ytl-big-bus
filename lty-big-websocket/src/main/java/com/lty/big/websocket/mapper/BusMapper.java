package com.lty.big.websocket.mapper;

import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface BusMapper {
	/*
	 * List<Map<String, Object>> queryBusStatistics(
	 * 
	 * @Param("cityCode") String cityCode);
	 */
}
